<Page
    x:Class="LiveCaptionsTranslator.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LiveCaptionsTranslator"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingPage"
    JournalEntry.KeepAlive="False"
    mc:Ignorable="d">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical">
            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.LiveCaptions}" />
                    <ui:Flyout x:Name="LiveCaptionsInfoFlyout">
                        <TextBlock Width="300" TextWrapping="Wrap">
                            <Run Text="{DynamicResource SP.Fly.LiveCaptions.After}" />
                            <Run FontWeight="DemiBold" TextDecorations="Underline" Text="{DynamicResource SP.Fly.LiveCaptions.SourceLanguage}" />
                            <Run Text="{DynamicResource SP.Fly.LiveCaptions.In}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.LiveCaptions.Note}" />
                            <Run Text="{DynamicResource SP.Fly.LiveCaptions.PleaseClick}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.LiveCaptions.HideQuoted}" />
                            <Run Text="{DynamicResource SP.Fly.LiveCaptions.ToHide}" />
                        </TextBlock>
                    </ui:Flyout>
                    <Button
                        x:Name="LiveCaptionsInfo"
                        Width="15"
                        Height="15"
                        Margin="-5,-3,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="LiveCaptionsInfo_MouseEnter"
                        MouseLeave="LiveCaptionsInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <ui:Button
                    x:Name="LiveCaptionsButton"
                    Width="120"
                    Height="30"
                    Padding="11,4,11,4"
                    Click="LiveCaptionsButton_click">
                    <TextBlock x:Name="ButtonText" Text="{DynamicResource SP.Show}" />
                </ui:Button>
            </StackPanel>

            <StackPanel Margin="15,10,0,0" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.ApiInterval}" />
                    <ui:Flyout x:Name="FrequencyInfoFlyout">
                        <TextBlock Width="300" TextWrapping="Wrap">
                            <Run Text="{DynamicResource SP.Fly.Frequency.Determines}" />
                            <Run Text="{DynamicResource SP.Fly.Frequency.Called}" />
                            <Run FontWeight="SemiBold" Text="{DynamicResource SP.Fly.Frequency.ApiInterval}" />
                            <Run Text="{DynamicResource SP.Fly.Frequency.Times}" />
                        </TextBlock>
                    </ui:Flyout>
                    <Button
                        x:Name="FrequencyInfo"
                        Width="15"
                        Height="15"
                        Margin="-5,-3,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="FrequencyInfo_MouseEnter"
                        MouseLeave="FrequencyInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Slider
                        Width="120"
                        Margin="0,0,5,0"
                        AutoToolTipPlacement="TopLeft"
                        AutoToolTipPrecision="0"
                        Maximum="10"
                        Value="{Binding MaxSyncInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Vertical">
            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.TranslateApi}" />
                    <ui:Flyout x:Name="TranslateAPIInfoFlyout">
                        <TextBlock
                            Width="300"
                            Text="{DynamicResource SP.Fly.TranslateApi.Text}"
                            TextWrapping="Wrap" />
                    </ui:Flyout>
                    <Button
                        x:Name="TranslateAPIInfo"
                        Width="15"
                        Height="15"
                        Margin="-5,-3,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="TranslateAPIInfo_MouseEnter"
                        MouseLeave="TranslateAPIInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <ComboBox
                    x:Name="TranslateAPIBox"
                    Width="150"
                    Height="30"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    SelectedItem="{Binding ApiName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    SelectionChanged="TranslateAPIBox_SelectionChanged" />
            </StackPanel>

            <StackPanel Margin="15,10,0,0" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock
                        Margin="2.5,0,0,5"
                        VerticalAlignment="Center"
                        Text="{DynamicResource SP.TargetLanguage}" />
                    <ui:Flyout x:Name="TargetLangInfoFlyout">
                        <TextBlock Width="350" TextWrapping="Wrap">
                            <Run FontWeight="DemiBold" TextDecorations="Underline" Text="{DynamicResource SP.Fly.TargetLang.Quote}" />
                            <Run Text="{DynamicResource SP.Fly.TargetLang.Edit}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.TargetLang.Bcp}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.TargetLang.Note}" />
                            <Run Text="{DynamicResource SP.Fly.TargetLang.NoteText}" />
                            <Run Text="{DynamicResource SP.Fly.TargetLang.NoNeed}" />
                        </TextBlock>
                    </ui:Flyout>
                    <Button
                        x:Name="TargetLangInfo"
                        Width="15"
                        Height="15"
                        Margin="-5,-3,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="TargetLangInfo_MouseEnter"
                        MouseLeave="TargetLangInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <ComboBox
                    x:Name="TargetLangBox"
                    Width="150"
                    Height="30"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    IsEditable="True"
                    LostFocus="TargetLangBox_LostFocus"
                    SelectionChanged="TargetLangBox_SelectionChanged" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Vertical">
            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.ApiSetting}" />
                </StackPanel>
                <ui:Button
                    x:Name="APISettingButton"
                    Width="100"
                    Height="30"
                    Padding="11,4,11,4"
                    Click="APISettingButton_click">
                    <TextBlock Text="{DynamicResource SP.Open}" />
                </ui:Button>
            </StackPanel>
            <StackPanel Margin="15,10,0,0" Orientation="Vertical">
                <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.ShowLatency}" />
                <ui:ToggleSwitch
                    Width="100"
                    Height="30"
                    Padding="10,4,10,7"
                    IsChecked="{Binding MainWindow.LatencyShow, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    OffContent="{DynamicResource Common.Off}"
                    OnContent="{DynamicResource Common.On}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="3" Orientation="Vertical">
            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.Contexts}" />
                    <ui:Flyout x:Name="CaptionLogMaxInfoFlyout">
                        <TextBlock Width="300" TextWrapping="Wrap">
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.Contexts.Contexts}" />
                            <Run Text="{DynamicResource SP.Fly.Contexts.Determines}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.Contexts.ContextAware}" />
                            <Run Text="{DynamicResource SP.Fly.Contexts.Enabled}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.Contexts.Overlay}" />
                            <Run Text="{DynamicResource SP.Fly.Contexts.Cards}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.Contexts.LogCards}" />
                            <Run Text="{DynamicResource SP.Fly.Contexts.OverlayEnabled}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.Contexts.Note}" />
                            <Run Text="{DynamicResource SP.Fly.Contexts.MustBe}" />
                            <Run FontWeight="Bold" Text="{DynamicResource SP.Fly.Contexts.Gte}" />
                            <Run Text="{DynamicResource SP.Fly.Contexts.Display}" />
                        </TextBlock>
                    </ui:Flyout>
                    <Button
                        x:Name="CaptionLogMaxInfo"
                        Width="15"
                        Height="15"
                        Margin="-5,-3,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="CaptionLogMaxInfo_MouseEnter"
                        MouseLeave="CaptionLogMaxInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                </StackPanel>
                <ui:NumberBox
                    Width="135"
                    Height="30"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    ClearButtonEnabled="False"
                    AcceptsExpression="False"
                    SpinButtonPlacementMode="Inline"
                    Maximum="10"
                    Minimum="1"
                    LargeChange="1"
                    SmallChange="1"
                    Value="{Binding NumContexts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    ValueChanged="Contexts_ValueChanged" />
            </StackPanel>

            <StackPanel Margin="15,10,0,0" Orientation="Vertical">
                <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.DisplaySentences}" />
                <ui:NumberBox
                    Width="135"
                    Height="30"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    ClearButtonEnabled="False"
                    AcceptsExpression="False"
                    SpinButtonPlacementMode="Inline"
                    Maximum="6"
                    Minimum="0"
                    LargeChange="1"
                    SmallChange="1"
                    Value="{Binding DisplaySentences, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    ValueChanged="DisplaySentences_ValueChanged" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="4" Orientation="Vertical">
            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.ContextAware}" />
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <ui:ToggleSwitch
                        Width="90"
                        Height="30"
                        Padding="10,4,10,7"
                        IsChecked="{Binding ContextAware, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        OffContent="{DynamicResource Common.Off}"
                        OnContent="{DynamicResource Common.On}" />
                    <Button
                        x:Name="ContextAwareInfo"
                        Width="15"
                        Height="15"
                        Margin="-5,-3,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        MouseEnter="ContextAwareInfo_MouseEnter"
                        MouseLeave="ContextAwareInfo_MouseLeave">
                        <ui:SymbolIcon Symbol="Info16" />
                    </Button>
                    <ui:Flyout x:Name="ContextAwareInfoFlyout">
                        <TextBlock Width="300" TextWrapping="Wrap">
                            <Run Text="{DynamicResource SP.Fly.ContextAware.Translate}" />
                            <Run Text="{DynamicResource SP.Fly.ContextAware.Accuracy}" />
                        </TextBlock>
                    </ui:Flyout>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="5" Margin="15,15,0,0" Orientation="Horizontal" VerticalAlignment="Top">
            <StackPanel Orientation="Vertical">
                <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.VoiceInputMode}" />
                <ComboBox
                    x:Name="VoiceInputModeBox"
                    Width="150"
                    Height="30"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    SelectionChanged="VoiceInputModeBox_SelectionChanged">
                    <ComboBoxItem Content="{DynamicResource SP.Mode.Off}" Tag="0" />
                    <ComboBoxItem Content="{DynamicResource SP.Mode.Translated}" Tag="1" />
                    <ComboBoxItem Content="{DynamicResource SP.Mode.Original}" Tag="2" />
                </ComboBox>
            </StackPanel>

            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.TypeIncomplete}" />
                <ui:ToggleSwitch
                    Width="110"
                    Height="30"
                    Padding="10,4,10,7"
                    Checked="VoiceInputHalfSentence_Changed"
                    IsChecked="{Binding VoiceInputHalfSentence, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    OffContent="{DynamicResource Common.Off}"
                    OnContent="{DynamicResource Common.On}"
                    Unchecked="VoiceInputHalfSentence_Changed" />
            </StackPanel>

            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.ToggleHotkey}" />
                <ui:Button
                    x:Name="VoiceInputHotkeyButton"
                    Width="150"
                    Height="30"
                    Padding="11,4,11,4"
                    Click="VoiceInputHotkeyButton_Click"
                    Focusable="True"
                    LostFocus="VoiceInputHotkeyButton_LostFocus"
                    PreviewKeyDown="VoiceInputHotkeyButton_PreviewKeyDown">
                    <TextBlock x:Name="VoiceInputHotkeyText" Text="Ctrl+Alt+V" />
                </ui:Button>
            </StackPanel>

            <StackPanel Margin="15,0,0,0" Orientation="Vertical">
                <TextBlock Margin="2.5,0,0,5" Text="{DynamicResource SP.Language}" />
                <ComboBox
                    x:Name="LanguageBox"
                    Width="110"
                    Height="30"
                    Padding="10,4,10,7"
                    FontSize="13.3"
                    SelectionChanged="LanguageBox_SelectionChanged">
                    <ComboBoxItem Content="English" Tag="en" />
                    <ComboBoxItem Content="中文" Tag="zh-CN" />
                </ComboBox>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
